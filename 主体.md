# OA 协同办公系统 — 主体框架组织结构

> 基于 Spring Cloud 微服务架构，包含 8 个模块（6 个公共模块 + 1 个网关 + 6 个业务服务）。

---

## 一、项目整体目录树

```
oa-cloud/                                   # 父工程（Maven 多模块聚合）
├── pom.xml                                 # 父 POM：统一版本管理 + 子模块声明
├── 设计.md                                 # 多产品方案对比文档
├── OA协同.md                               # OA 系统完整需求 + 技术设计文档
├── 主体.md                                 # 本文档：主体框架组织结构
│
├── oa-common/                              # 公共模块聚合（POM 类型，不打包）
│   ├── pom.xml
│   ├── oa-common-core/                     # 核心工具模块
│   ├── oa-common-redis/                    # 缓存与分布式锁模块
│   ├── oa-common-jwt/                      # JWT 工具模块（无 Web 依赖）
│   ├── oa-common-security/                 # JWT 鉴权与权限模块
│   ├── oa-common-rabbitmq/                 # 消息队列模块
│   └── oa-common-mybatis/                  # 数据库访问模块
│
├── oa-gateway/                             # API 网关服务（端口 8080）
├── oa-auth/                                # 用户权限服务（端口 8081）
├── oa-approval/                            # 审批服务（端口 8082）
├── oa-attendance/                          # 考勤服务（端口 8083）
├── oa-project/                             # 项目任务服务（端口 8084）
├── oa-document/                            # 文档知识库服务（端口 8085）
└── oa-notification/                        # 通知服务（端口 8086）
```

---

## 二、父 POM — 版本管理与模块聚合

**文件：** `pom.xml`

| 配置项 | 值 | 说明 |
|--------|------|------|
| parent | spring-boot-starter-parent 4.0.7 | 继承 Spring Boot |
| packaging | pom | 仅做模块聚合，不打包 |
| spring-cloud.version | 2025.0.0 | Spring Cloud BOM 版本 |
| spring-cloud-alibaba.version | 2023.0.3.2 | Nacos / Sentinel 等 |
| mybatis-plus.version | 3.5.12 | MyBatis-Plus ORM |
| redisson.version | 3.43.0 | Redisson 分布式锁 |
| jjwt.version | 0.12.6 | JWT Token 生成/解析 |
| caffeine.version | 3.2.0 | Caffeine 本地缓存 |
| hutool.version | 5.8.36 | Hutool 工具包 |

**dependencyManagement 声明的内部模块：**
- `oa-common-core` / `oa-common-redis` / `oa-common-jwt` / `oa-common-security` / `oa-common-rabbitmq` / `oa-common-mybatis`

---

## 三、公共模块详解（oa-common）

### 3.1 oa-common-core — 核心工具模块

```
oa-common-core/
├── pom.xml                                 # 依赖：spring-web(optional)、lombok、hutool、jackson
└── src/main/java/cn/qiao/oa/common/core/
    ├── result/
    │   ├── R.java                          # 统一响应结果封装
    │   └── PageResult.java                 # 分页结果封装
    ├── exception/
    │   ├── BusinessException.java          # 业务异常类
    │   └── GlobalExceptionHandler.java     # 全局异常处理器
    └── constant/
        └── CommonConstant.java             # 公共常量定义
```

| 类名 | 功能说明 |
|------|----------|
| **R\<T\>** | 统一 API 响应封装。`ok()` 返回成功，`fail()` 返回失败，保证前后端接口格式一致。字段：code（状态码）、message（提示）、data（数据） |
| **PageResult\<T\>** | 分页查询结果封装。字段：total（总记录数）、records（当前页数据）、current（页码）、size（每页大小） |
| **BusinessException** | 自定义业务异常，携带 code + message。Service 层抛出，由全局处理器统一捕获 |
| **GlobalExceptionHandler** | `@RestControllerAdvice` 全局异常处理器。处理 BusinessException（业务异常）、MethodArgumentNotValidException（参数校验）、BindException（绑定异常）、Exception（未知异常） |
| **CommonConstant** | 公共常量：SUPER_ADMIN（超级管理员角色）、STATUS_ENABLE/DISABLE（状态值）、REDIS_PREFIX（Redis Key 前缀）、TOKEN_PREFIX/TOKEN_HEADER（Token 相关） |

---

### 3.2 oa-common-redis — 缓存与分布式锁模块

```
oa-common-redis/
├── pom.xml                                 # 依赖：oa-common-core、spring-data-redis、redisson、caffeine
└── src/main/java/cn/qiao/oa/common/redis/
    ├── config/
    │   └── RedisTemplateConfig.java        # Redis 序列化配置
    ├── cache/
    │   └── CacheService.java               # 多级缓存服务
    └── lock/
        └── DistributedLock.java            # Redisson 分布式锁工具
```

| 类名 | 功能说明 |
|------|----------|
| **RedisTemplateConfig** | 自定义 `RedisTemplate<String, Object>` 的序列化方式。Key 使用 StringRedisSerializer，Value 使用 GenericJackson2JsonRedisSerializer（支持 LocalDateTime） |
| **CacheService** | **多级缓存核心组件**（Caffeine 本地缓存 + Redis 分布式缓存）。解决三大缓存问题：①**缓存穿透**：缓存空值（NULL_PLACEHOLDER）；②**缓存击穿**：synchronized 加锁 + 双重检查；③**缓存雪崩**：TTL 随机偏移 ±20%。核心方法：`get(key, clazz, expiration, supplier)` |
| **DistributedLock** | Redisson 分布式锁封装。提供三种锁模式：①`executeWithLock()` — 基础锁（看门狗自动续期）；②`executeWithLock(waitTime, leaseTime, unit)` — 带超时参数的锁；③`executeWithFairLock()` — 公平锁（排队场景） |

**CacheService 方法清单：**

| 方法 | 说明 |
|------|------|
| `get(key, clazz, expiration, supplier)` | 多级缓存查询：Caffeine → Redis → DB（加锁） |
| `get(key, clazz, supplier)` | 简化版查询（默认 30 分钟过期） |
| `put(key, value, expiration)` | 手动写入缓存 |
| `evict(key)` | 删除缓存（同时删 Caffeine + Redis） |
| `clearLocal()` | 清空所有本地缓存 |

---

### 3.3 oa-common-jwt — JWT 工具模块（无 Web 依赖）

```
oa-common-jwt/
├── pom.xml                                 # 依赖：spring-context、jjwt-api/impl/jackson（无 webmvc/webflux）
└── src/main/java/cn/qiao/oa/common/jwt/
    └── utils/
        └── JwtUtils.java                   # JWT 工具类
```

| 类名 | 功能说明 |
|------|----------|
| **JwtUtils** | JWT Token 工具类。`generateToken(userId, username)` 生成 Token；`parseToken(token)` 解析 Claims；`getUserId(token)` / `getUsername(token)` 提取信息；`validateToken(token)` 验证有效性。密钥通过 `@Value("${oa.jwt.secret}")` 从配置文件读取 |

> **设计目的：** 将 JWT 工具从 `oa-common-security` 中抽离为独立模块，不含任何 Web 依赖（WebMVC/WebFlux），使网关（WebFlux）和业务服务（WebMVC）可以共用同一份 JWT 代码，彻底避免依赖冲突。

---

### 3.4 oa-common-security — JWT 鉴权与权限模块

```
oa-common-security/
├── pom.xml                                 # 依赖：oa-common-core、oa-common-redis、oa-common-jwt、spring-webmvc、spring-aop
└── src/main/java/cn/qiao/oa/common/security/
    ├── utils/
    │   └── SecurityUtils.java              # 用户上下文（ThreadLocal）
    ├── interceptor/
    │   └── AuthInterceptor.java            # 用户认证拦截器
    ├── config/
    │   └── WebMvcConfig.java               # Web MVC 配置
    ├── annotation/
    │   └── RequiresPermission.java         # 权限校验注解
    └── aspect/
        └── PermissionAspect.java           # 权限校验切面
```

| 类名 | 功能说明 |
|------|----------|
| **SecurityUtils** | 基于 ThreadLocal 的用户上下文。内部类 `LoginUser` 包含：userId、username、deptId、permissions、roles、dataScope。提供 `setLoginUser()`、`getLoginUser()`、`getUserId()`、`clear()` 方法 |
| **AuthInterceptor** | `HandlerInterceptor` 实现。从 Gateway 传递的 Header（X-User-Id、X-Username、X-Dept-Id、X-Data-Scope）中提取用户信息，写入 SecurityUtils ThreadLocal。请求完成后自动清理 |
| **WebMvcConfig** | `WebMvcConfigurer` 实现。注册 AuthInterceptor 拦截所有请求；配置 CORS 跨域策略（开发环境允许所有来源） |
| **@RequiresPermission** | 接口权限校验注解。使用示例：`@RequiresPermission("approval:audit:approve")` |
| **PermissionAspect** | AOP 切面，拦截 `@RequiresPermission` 注解。校验逻辑：超级管理员（super_admin）直接放行 → 检查用户权限列表是否包含目标权限 → 不通过抛出 403 异常 |

---

### 3.5 oa-common-rabbitmq — 消息队列模块

```
oa-common-rabbitmq/
├── pom.xml                                 # 依赖：oa-common-core、spring-amqp
└── src/main/java/cn/qiao/oa/common/rabbitmq/
    ├── config/
    │   └── RabbitMQConfig.java             # 队列、交换机、绑定关系声明
    └── message/
        ├── ApprovalNotifyMessage.java      # 审批通知消息 DTO
        ├── TaskNotifyMessage.java          # 任务通知消息 DTO
        └── AttendanceNotifyMessage.java    # 考勤通知消息 DTO
```

| 类名 | 功能说明 |
|------|----------|
| **RabbitMQConfig** | 统一定义队列、交换机和绑定关系。队列：`oa.notification.queue`（通知队列）、`oa.notification.delay.queue`（延迟队列）、`oa.notification.dead.queue`（死信队列）。交换机：Topic 模式，路由键 `notify.{type}.*` |
| **ApprovalNotifyMessage** | 审批通知消息体。字段：action（submit/approve/reject/withdraw/urge）、instanceId（审批单 ID）、targetUserId（接收人）、title |
| **TaskNotifyMessage** | 任务通知消息体。字段：action（assign/deadline/status_change/comment）、taskId、projectId、targetUserId、title、content |
| **AttendanceNotifyMessage** | 考勤通知消息体。字段：action（late/absent/overtime_result/leave_result）、recordId、targetUserId、title、content |

**RabbitMQ 队列规划：**

| 队列 | 类型 | 说明 |
|------|------|------|
| `oa.notification.queue` | 持久化 | 主通知队列，绑定死信交换机 |
| `oa.notification.delay.queue` | 持久化 | 延迟队列（TTL 过期后转发到通知交换机） |
| `oa.notification.dead.queue` | 持久化 | 死信队列（消费失败消息，供人工排查） |
| `oa.notification.exchange` | Topic | 通知交换机，路由键：`notify.approval.*` / `notify.task.*` / `notify.attendance.*` |
| `oa.notification.delay.exchange` | Topic | 延迟交换机 |
| `oa.notification.dead.exchange` | Topic | 死信交换机 |

---

### 3.6 oa-common-mybatis — 数据库访问模块

```
oa-common-mybatis/
├── pom.xml                                 # 依赖：oa-common-core、mybatis-plus、mysql-driver、spring-aop、oa-common-security(optional)
└── src/main/java/cn/qiao/oa/common/mybatis/
    ├── config/
    │   └── MybatisPlusConfig.java          # MyBatis-Plus 配置
    ├── entity/
    │   └── BaseEntity.java                 # 公共实体基类
    ├── handler/
    │   └── AutoFillHandler.java            # 自动填充处理器
    ├── annotation/
    │   └── DataScope.java                  # 数据权限注解
    └── aspect/
        ├── DataScopeAspect.java            # 数据权限切面
        └── DataScopeContextHolder.java     # 数据权限 SQL 条件持有者
```

| 类名 | 功能说明 |
|------|----------|
| **MybatisPlusConfig** | 注册 `PaginationInnerInterceptor` 分页插件（DbType.MYSQL） |
| **BaseEntity** | 公共实体基类（所有业务实体继承）。提供字段：id（雪花算法）、createBy/createTime（INSERT 自动填充）、updateBy/updateTime（INSERT_UPDATE 自动填充）、deleted（逻辑删除） |
| **AutoFillHandler** | `MetaObjectHandler` 实现。INSERT 时自动填充 createTime/updateTime/createBy/updateBy；UPDATE 时自动填充 updateTime/updateBy。操作人从 SecurityUtils 获取 |
| **@DataScope** | 数据权限注解。参数：`deptAlias`（部门表别名）、`userAlias`（用户表别名） |
| **DataScopeAspect** | 数据权限 AOP 切面。根据用户 dataScope 值动态拼接 SQL：1=全部（不过滤）、2=本部门、3=本部门及子部门（FIND_IN_SET）、4=仅本人 |
| **DataScopeContextHolder** | ThreadLocal 持有数据权限 SQL 条件，供 Mapper 层 MyBatis 拦截器读取 |

---

## 四、网关服务（oa-gateway）

```
oa-gateway/                                 # 端口 8080
├── pom.xml                                 # 依赖：spring-cloud-gateway、spring-data-redis-reactive、nacos-discovery、oa-common-jwt
└── src/main/
    ├── java/cn/qiao/oa/gateway/
    │   ├── GatewayApplication.java         # 网关启动类
    │   ├── config/
    │   │   └── GatewayProperties.java      # 网关配置属性
    │   └── filter/
    │       └── AuthGlobalFilter.java       # 全局鉴权过滤器
    └── resources/
        └── application.yml                 # 路由规则 + 白名单配置
```

| 类名 | 功能说明 |
|------|----------|
| **GatewayApplication** | Spring Cloud Gateway 启动类。`@EnableDiscoveryClient` 注册到 Nacos |
| **GatewayProperties** | `@ConfigurationProperties(prefix="gateway")` 配置类。字段：`whitelist`（白名单路径列表，免鉴权） |
| **AuthGlobalFilter** | `GlobalFilter` 全局鉴权过滤器。注入 `JwtUtils`（来自 oa-common-jwt）解析 Token。流程：①检查白名单放行 → ②获取 Authorization Header → ③JwtUtils 验证 + 解析 Token → ④将 userId/username 写入 X-User-Id/X-Username Header 传递给下游 |

**路由规则（application.yml）：**

| 路由 ID | URI | 匹配路径 | 目标服务 |
|---------|-----|----------|----------|
| oa-auth | lb://oa-auth | /auth/** | 用户权限服务 |
| oa-approval | lb://oa-approval | /approval/** | 审批服务 |
| oa-attendance | lb://oa-attendance | /attendance/** | 考勤服务 |
| oa-project | lb://oa-project | /project/** | 项目服务 |
| oa-document | lb://oa-document | /document/** | 文档服务 |
| oa-notification | lb://oa-notification | /notification/** | 通知服务 |

**白名单路径：** `/auth/login`、`/auth/register`、`/auth/captcha`

---

## 五、业务微服务

### 5.1 oa-auth — 用户权限服务（端口 8081）

```
oa-auth/                                    # 端口 8081，数据库：oa_auth
├── pom.xml                                 # 依赖：core + redis + security + mybatis + nacos + openfeign
└── src/main/
    ├── java/cn/qiao/oa/auth/
    │   ├── AuthApplication.java            # 启动类（@MapperScan + @EnableFeignClients）
    │   ├── controller/                     # REST 控制器（待实现）
    │   ├── service/                        # 业务接口（待实现）
    │   │   └── impl/                       # 业务实现（待实现）
    │   ├── mapper/                         # MyBatis Mapper（待实现）
    │   ├── entity/                         # 数据库实体（待实现）
    │   └── dto/                            # 数据传输对象（待实现）
    └── resources/
        └── application.yml                 # MySQL + Redis + Nacos + MyBatis-Plus 配置
```

**职责：** 用户管理（注册/登录/个人信息）、部门管理（组织架构树）、角色管理（RBAC）、菜单管理（权限标识绑定）

---

### 5.2 oa-approval — 审批服务（端口 8082）

```
oa-approval/                                # 端口 8082，数据库：oa_approval
├── pom.xml                                 # 依赖：core + redis + security + mybatis + rabbitmq + nacos + openfeign
└── src/main/
    ├── java/cn/qiao/oa/approval/
    │   ├── ApprovalApplication.java        # 启动类
    │   ├── controller/                     # REST 控制器（待实现）
    │   ├── service/                        # 业务接口（待实现）
    │   │   └── impl/                       # 业务实现（待实现）
    │   ├── mapper/                         # MyBatis Mapper（待实现）
    │   ├── entity/                         # 数据库实体（待实现）
    │   └── dto/                            # 数据传输对象（待实现）
    └── resources/
        └── application.yml                 # MySQL + Redis + RabbitMQ + Nacos 配置
```

**职责：** 审批模板管理、审批实例流转（提交/审批/撤回/催办）、多级审批节点（会签/或签/条件分支）

---

### 5.3 oa-attendance — 考勤服务（端口 8083）

```
oa-attendance/                              # 端口 8083，数据库：oa_attendance
├── pom.xml                                 # 依赖：core + redis + security + mybatis + rabbitmq + nacos + openfeign
└── src/main/
    ├── java/cn/qiao/oa/attendance/
    │   ├── AttendanceApplication.java      # 启动类
    │   ├── controller/                     # REST 控制器（待实现）
    │   ├── service/                        # 业务接口（待实现）
    │   │   └── impl/                       # 业务实现（待实现）
    │   ├── mapper/                         # MyBatis Mapper（待实现）
    │   ├── entity/                         # 数据库实体（待实现）
    │   └── dto/                            # 数据传输对象（待实现）
    └── resources/
        └── application.yml                 # MySQL + Redis + RabbitMQ + Nacos 配置
```

**职责：** 打卡管理（上班/下班/外勤）、考勤规则配置（弹性/固定/排班）、考勤统计（迟到/早退/缺卡/月度报表）

---

### 5.4 oa-project — 项目任务服务（端口 8084）

```
oa-project/                                 # 端口 8084，数据库：oa_project
├── pom.xml                                 # 依赖：core + redis + security + mybatis + rabbitmq + nacos + openfeign
└── src/main/
    ├── java/cn/qiao/oa/project/
    │   ├── ProjectApplication.java         # 启动类
    │   ├── controller/                     # REST 控制器（待实现）
    │   ├── service/                        # 业务接口（待实现）
    │   │   └── impl/                       # 业务实现（待实现）
    │   ├── mapper/                         # MyBatis Mapper（待实现）
    │   ├── entity/                         # 数据库实体（待实现）
    │   └── dto/                            # 数据传输对象（待实现）
    └── resources/
        └── application.yml                 # MySQL + Redis + RabbitMQ + Nacos 配置
```

**职责：** 项目管理（创建/编辑/归档）、任务管理（分配/完成/看板）、项目统计（完成率/甘特图）

---

### 5.5 oa-document — 文档知识库服务（端口 8085）

```
oa-document/                                # 端口 8085，数据库：oa_document
├── pom.xml                                 # 依赖：core + redis + security + mybatis + nacos（无 RabbitMQ）
└── src/main/
    ├── java/cn/qiao/oa/document/
    │   ├── DocumentApplication.java        # 启动类
    │   ├── controller/                     # REST 控制器（待实现）
    │   ├── service/                        # 业务接口（待实现）
    │   │   └── impl/                       # 业务实现（待实现）
    │   ├── mapper/                         # MyBatis Mapper（待实现）
    │   └── entity/                         # 数据库实体（待实现）
    └── resources/
        └── application.yml                 # MySQL + Redis + Nacos 配置
```

**职责：** 文档管理（上传/下载/预览/删除）、文件夹管理（多级目录树）、文档权限控制、版本管理

---

### 5.6 oa-notification — 通知服务（端口 8086）

```
oa-notification/                            # 端口 8086，数据库：oa_notification
├── pom.xml                                 # 依赖：core + redis + security + mybatis + rabbitmq + nacos
└── src/main/
    ├── java/cn/qiao/oa/notification/
    │   ├── NotificationApplication.java    # 启动类
    │   ├── controller/                     # REST 控制器（待实现）
    │   ├── service/                        # 业务接口（待实现）
    │   │   └── impl/                       # 业务实现（待实现）
    │   ├── mapper/                         # MyBatis Mapper（待实现）
    │   ├── entity/                         # 数据库实体（待实现）
    │   └── consumer/                       # RabbitMQ 消息消费者（待实现）
    └── resources/
        └── application.yml                 # MySQL + Redis + RabbitMQ + Nacos 配置
```

**职责：** RabbitMQ 消息消费（审批/任务/考勤通知）、站内信管理（创建/查询/已读/删除）、WebSocket 实时推送

---

## 六、模块依赖关系

```
                    oa-common-core（核心工具）
                   /      |      |       \
                  /       |      |        \
    oa-common-redis  oa-common-security  oa-common-rabbitmq  oa-common-mybatis
         |              |      |              |                    |
         |              |      |              |                    |
         └──────────────┘      |              |                    |
                ↑              |              |                    |
       security 依赖 redis     |              |                    |
                               |              |                    |
                    oa-common-security         |                    |
                        (interceptor)          |                    |
                            |                  |                    |
                            └──── optional ────┘────────────────────┘
                                    mybatis 可选依赖 security
```

**业务服务依赖矩阵：**

| 服务 | core | redis | security | mybatis | rabbitmq | openfeign |
|------|:----:|:-----:|:--------:|:-------:|:--------:|:---------:|
| oa-auth | ✅ | ✅ | ✅ | ✅ | — | ✅ |
| oa-approval | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| oa-attendance | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| oa-project | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| oa-document | ✅ | ✅ | ✅ | ✅ | — | — |
| oa-notification | ✅ | ✅ | ✅ | ✅ | ✅ | — |

---

## 七、技术栈版本汇总

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 4.0.7 | 基础框架 |
| Spring Cloud | 2025.0.0 | 微服务框架 |
| Spring Cloud Alibaba | 2023.0.3.2 | Nacos 服务发现/配置中心 |
| Spring Cloud Gateway | — | 响应式 API 网关 |
| OpenFeign | — | 声明式服务间调用 |
| MyBatis-Plus | 3.5.12 | ORM 框架（分页、逻辑删除、自动填充） |
| MySQL | 8.x | 关系型数据库 |
| Redis | — | 分布式缓存 |
| Redisson | 3.43.0 | 分布式锁、布隆过滤器 |
| Caffeine | 3.2.0 | 本地缓存（多级缓存方案） |
| RabbitMQ | — | 消息队列（延迟队列/死信队列） |
| jjwt | 0.12.6 | JWT Token 生成/解析 |
| Hutool | 5.8.36 | Java 工具包 |
| Lombok | — | 代码简化（getter/setter/builder） |
| Java | 17 | JDK 版本 |

---

## 八、请求处理流程

```
前端请求 → Nginx（反向代理）
    → oa-gateway:8080（全局鉴权）
        → AuthGlobalFilter：JWT 校验 → 解析 userId/username → 写入 Header
        → 路由到目标微服务
            → AuthInterceptor：从 Header 提取用户信息 → 写入 SecurityUtils ThreadLocal
            → Controller → Service（@RequiresPermission 权限校验）
                → Mapper（@DataScope 数据权限过滤）
                → CacheService（多级缓存查询）
                → DistributedLock（分布式锁保护写操作）
                → RabbitMQ（异步发送通知消息）
                    → oa-notification consumer（消费通知 → 写入数据库 → WebSocket 推送）
```

---

## 九、文件统计

| 模块 | Java 文件数 | 配置文件数 |
|------|:-----------:|:----------:|
| oa-common-core | 5 | 1 (pom.xml) |
| oa-common-redis | 3 | 1 (pom.xml) |
| oa-common-security | 6 | 1 (pom.xml) |
| oa-common-rabbitmq | 4 | 1 (pom.xml) |
| oa-common-mybatis | 6 | 1 (pom.xml) |
| oa-gateway | 3 | 2 (pom.xml + yml) |
| oa-auth | 1 | 2 (pom.xml + yml) |
| oa-approval | 1 | 2 (pom.xml + yml) |
| oa-attendance | 1 | 2 (pom.xml + yml) |
| oa-project | 1 | 2 (pom.xml + yml) |
| oa-document | 1 | 2 (pom.xml + yml) |
| oa-notification | 1 | 2 (pom.xml + yml) |
| 父 POM | — | 1 (pom.xml) |
| **合计** | **33** | **20** |
