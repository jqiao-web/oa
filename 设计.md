# Spring Cloud 微服务求职项目 — 产品与技术方案选型

> 本文档提供 **5 个**不同方向的产品方案，每个方案均包含需求概述、核心功能模块、技术架构设计及亮点说明。  
> 请根据个人兴趣和目标岗位选择一个方向进行开发。

---

## 一、统一技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 后端框架 | Spring Boot 4.x + Spring Cloud 2024.x | 微服务核心框架 |
| 微服务治理 | Spring Cloud Alibaba (Nacos) | 注册中心 + 配置中心 |
| 数据库 | MySQL 8.x | 关系型数据库 |
| ORM | MyBatis-Plus | 简化 CRUD 操作 |
| 缓存 | Redis | 高性能缓存 + 分布式锁 |
| 消息队列 | RabbitMQ | 异步解耦 + 削峰填谷 |
| 网关 | Spring Cloud Gateway | 统一入口、路由、鉴权 |
| 服务调用 | OpenFeign | 声明式 HTTP 客户端 |
| 容器化 | Docker + Docker Compose | 一键部署所有服务 |
| 反向代理 | Nginx | 前端静态资源 + API 转发 |
| 前端框架 | Vue 3 + TypeScript | 组合式 API + 类型安全 |
| UI 组件库 | Element Plus | 企业级组件库 |
| 状态管理 | Pinia | Vue 3 官方状态管理 |
| 构建工具 | Vite | 极速前端构建 |

---

## 二、通用架构设计（所有方案共用）

```
                           ┌─────────────────┐
                           │     Nginx       │
                           │  (反向代理/静态) │
                           └────────┬────────┘
                                    │
                     ┌──────────────┼──────────────┐
                     │              │              │
               ┌─────┴─────┐ ┌─────┴─────┐ ┌─────┴─────┐
               │ 前端静态   │ │  API 请求  │ │ WebSocket │
               │ 资源(Vue) │ │  转发网关  │ │  长连接    │
               └───────────┘ └─────┬─────┘ └───────────┘
                                   │
                         ┌─────────┴─────────┐
                         │ Spring Cloud      │
                         │ Gateway 网关      │
                         │ (路由/鉴权/限流)   │
                         └─────────┬─────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
       ┌──────┴──────┐     ┌──────┴──────┐     ┌──────┴──────┐
       │  微服务 A   │     │  微服务 B   │     │  微服务 C   │
       │ (Nacos 注册)│     │ (Nacos 注册)│     │ (Nacos 注册)│
       └──┬───┬───┬──┘     └──┬───┬───┬──┘     └──┬───┬───┬──┘
          │   │   │           │   │   │           │   │   │
          │   │   └──► RabbitMQ (异步消息队列)      │   │   │
          │   └──────► Redis  (缓存/分布式锁)       │   │   │
          └──────────► MySQL  (持久化存储)           │   │   │
                                                   │   │   │
              ┌────────────────────────────────────┘   │   │
              │                                        │   │
              ▼                                        ▼   ▼
           MySQL / Redis / RabbitMQ                   (同上)
```

### Docker Compose 部署架构

```yaml
# docker-compose.yml 服务编排示意
services:
  nginx:          # 反向代理 + 前端静态资源
  gateway:        # Spring Cloud Gateway 网关
  user-service:   # 用户服务
  xxx-service:    # 业务微服务（根据方案不同）
  mysql:          # MySQL 8.x
  redis:          # Redis 7.x
  rabbitmq:       # RabbitMQ 3.x（含管理面板 15672）
  nacos:          # Nacos 注册/配置中心
```

---

## 三、Redis 缓存方案（通用设计，所有方案共用）

### 3.1 缓存穿透（Cache Penetration）

**问题**：查询一个**根本不存在**的 key，缓存和数据库都查不到，每次请求都直接打到数据库。

**解决方案**：

```java
/**
 * 缓存穿透解决方案
 * 1. 缓存空值（设置短过期时间）
 * 2. 布隆过滤器（BloomFilter）拦截不存在的 key
 */
@Service
public class CacheService {

    @Autowired
    private StringRedisTemplate redisTemplate;

    @Autowired
    private RedissonClient redissonClient;

    // ==================== 方案一：缓存空值 ====================
    public <T> T queryWithPassThrough(String keyPrefix, Long id, Class<T> type,
                                       Function<Long, T> dbFallback, Long ttl, TimeUnit unit) {
        String key = keyPrefix + id;
        // 1. 查缓存
        String json = redisTemplate.opsForValue().get(key);
        // 2. 判断是否存在（注意：空串 "" 也是缓存值，代表数据库中不存在）
        if (json != null) {
            return json.isEmpty() ? null : JSONUtil.toBean(json, type);
        }
        // 3. 缓存未命中，查数据库
        T result = dbFallback.apply(id);
        // 4. 数据库也查不到 → 缓存空值，设置短 TTL，防止穿透
        if (result == null) {
            redisTemplate.opsForValue().set(key, "", 2, TimeUnit.MINUTES);
            return null;
        }
        // 5. 写入缓存
        redisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(result), ttl, unit);
        return result;
    }

    // ==================== 方案二：布隆过滤器 ====================
    public void initBloomFilter(String filterName, List<Long> ids) {
        RBloomFilter<Long> bloomFilter = redissonClient.getBloomFilter(filterName);
        bloomFilter.tryInit(100000L, 0.03); // 预计元素量 + 误判率 3%
        ids.forEach(bloomFilter::add);
    }

    public boolean mightExist(String filterName, Long id) {
        RBloomFilter<Long> bloomFilter = redissonClient.getBloomFilter(filterName);
        return bloomFilter.contains(id); // false = 一定不存在，直接拒绝
    }
}
```

### 3.2 缓存击穿（Cache Breakdown）

**问题**：某个**热点 key** 过期的瞬间，大量并发请求同时打到数据库，造成数据库压力骤增。

**解决方案**：

```java
/**
 * 缓存击穿解决方案
 * 1. 互斥锁（Redisson 分布式锁）— 强一致性，只有一个线程去重建缓存
 * 2. 逻辑过期 — 高可用，允许短暂返回旧数据，异步重建
 */
@Service
public class CacheBreakdownService {

    @Autowired
    private StringRedisTemplate redisTemplate;

    @Autowired
    private RedissonClient redissonClient;

    // ==================== 方案一：互斥锁（推荐，保证强一致性）====================
    public <T> T queryWithMutex(String keyPrefix, Long id, Class<T> type,
                                 Function<Long, T> dbFallback, Long ttl, TimeUnit unit) {
        String key = keyPrefix + id;
        String lockKey = "lock:" + key;

        // 1. 查缓存
        String json = redisTemplate.opsForValue().get(key);
        if (json != null) {
            return json.isEmpty() ? null : JSONUtil.toBean(json, type);
        }

        // 2. 缓存未命中，获取分布式锁
        RLock lock = redissonClient.getLock(lockKey);
        T result = null;
        try {
            // 等待5秒，锁持有10秒自动释放
            boolean acquired = lock.tryLock(5, 10, TimeUnit.SECONDS);
            if (!acquired) {
                Thread.sleep(50); // 未获锁，短暂休眠后重试
                return queryWithMutex(keyPrefix, id, type, dbFallback, ttl, unit);
            }
            // 3. 双重检查（Double Check），防止重复查库
            json = redisTemplate.opsForValue().get(key);
            if (json != null) {
                return json.isEmpty() ? null : JSONUtil.toBean(json, type);
            }
            // 4. 查数据库并写缓存
            result = dbFallback.apply(id);
            if (result == null) {
                redisTemplate.opsForValue().set(key, "", 2, TimeUnit.MINUTES);
            } else {
                redisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(result), ttl, unit);
            }
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        } finally {
            if (lock.isHeldByCurrentThread()) {
                lock.unlock();
            }
        }
        return result;
    }

    // ==================== 方案二：逻辑过期（高可用，允许短暂不一致）====================
    public <T> T queryWithLogicalExpire(String keyPrefix, Long id, Class<T> type,
                                         Function<Long, T> dbFallback, Long expireSeconds) {
        String key = keyPrefix + id;
        String lockKey = "lock:" + key;

        String json = redisTemplate.opsForValue().get(key);
        if (json == null) return null;

        CacheData<T> cacheData = JSONUtil.toBean(json, new TypeReference<>() {}, false);

        // 判断是否逻辑过期
        if (cacheData.getExpireTime().isAfter(LocalDateTime.now())) {
            return cacheData.getData(); // 未过期，直接返回
        }

        // 已逻辑过期，尝试获取锁，异步重建缓存（未获到锁的线程返回旧数据）
        RLock lock = redissonClient.getLock(lockKey);
        if (lock.tryLock()) {
            try {
                // 再次检查，防止重复重建
                json = redisTemplate.opsForValue().get(key);
                cacheData = JSONUtil.toBean(json, new TypeReference<>() {}, false);
                if (cacheData.getExpireTime().isAfter(LocalDateTime.now())) {
                    return cacheData.getData();
                }
                // 重建缓存
                T result = dbFallback.apply(id);
                CacheData<T> newData = new CacheData<>();
                newData.setData(result);
                newData.setExpireTime(LocalDateTime.now().plusSeconds(expireSeconds));
                redisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(newData));
            } finally {
                lock.unlock();
            }
        }
        return cacheData.getData(); // 未获到锁，返回旧数据
    }
}

// 逻辑过期封装类
@Data
class CacheData<T> {
    private T data;
    private LocalDateTime expireTime;
}
```

### 3.3 缓存雪崩（Cache Avalanche）

**问题**：大量缓存 key 在**同一时间**过期，或 Redis 宕机，导致请求全部打到数据库。

**解决方案**：

```java
/**
 * 缓存雪崩解决方案
 * 1. TTL 加随机值，打散过期时间，防止同时过期
 * 2. Redis 高可用（主从 + 哨兵 / 集群模式）
 * 3. 多级缓存（Caffeine 本地缓存 + Redis）
 * 4. 熔断降级兜底（Sentinel / Resilience4j）
 */
@Service
public class CacheAvalancheService {

    @Autowired
    private StringRedisTemplate redisTemplate;

    private static final Random RANDOM = new Random();

    // ==================== 方案一：TTL 随机偏移 ====================
    public void setWithRandomTTL(String key, String value, int baseTTLSeconds) {
        // 在基础 TTL 上增加 0~300 秒的随机偏移
        int randomTTL = baseTTLSeconds + RANDOM.nextInt(300);
        redisTemplate.opsForValue().set(key, value, randomTTL, TimeUnit.SECONDS);
    }

    // ==================== 方案二：多级缓存（Caffeine + Redis）====================
    private final Cache<String, Object> localCache = Caffeine.newBuilder()
            .maximumSize(1000)
            .expireAfterWrite(5, TimeUnit.MINUTES) // 一级本地缓存 5 分钟
            .build();

    @SuppressWarnings("unchecked")
    public <T> T getWithMultiCache(String key, Class<T> type, Supplier<T> dbFallback) {
        // 1. 查一级缓存（本地 Caffeine）
        T result = (T) localCache.getIfPresent(key);
        if (result != null) return result;

        // 2. 查二级缓存（Redis）
        String json = redisTemplate.opsForValue().get(key);
        if (json != null) {
            result = JSONUtil.toBean(json, type);
            localCache.put(key, result); // 回填本地缓存
            return result;
        }

        // 3. 查数据库（兜底）
        result = dbFallback.get();
        if (result != null) {
            localCache.put(key, result);
            setWithRandomTTL(key, JSONUtil.toJsonStr(result), 3600); // 写 Redis，随机 TTL
        }
        return result;
    }
}
```

---

## 四、分布式锁方案（通用设计，所有方案共用）

### 4.1 基于 Redisson 的分布式锁封装

```java
/**
 * 分布式锁封装工具类
 * 基于 Redisson 实现，支持可重入、自动续期（看门狗）、公平锁等特性
 */
@Component
public class DistributedLock {

    @Autowired
    private RedissonClient redissonClient;

    /**
     * 基础分布式锁（自动续期，看门狗默认 30 秒）
     */
    public <T> T executeWithLock(String lockKey, Supplier<T> supplier) {
        RLock lock = redissonClient.getLock(lockKey);
        try {
            if (lock.tryLock(5, TimeUnit.SECONDS)) {
                try {
                    return supplier.get();
                } finally {
                    if (lock.isHeldByCurrentThread()) {
                        lock.unlock();
                    }
                }
            }
            throw new RuntimeException("获取分布式锁失败: " + lockKey);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new RuntimeException("获取分布式锁被中断", e);
        }
    }

    /**
     * 带超时的分布式锁（不自动续期，到期强制释放）
     */
    public <T> T executeWithLock(String lockKey, long waitTime, long leaseTime,
                                  TimeUnit unit, Supplier<T> supplier) {
        RLock lock = redissonClient.getLock(lockKey);
        try {
            if (lock.tryLock(waitTime, leaseTime, unit)) {
                try {
                    return supplier.get();
                } finally {
                    if (lock.isHeldByCurrentThread()) {
                        lock.unlock();
                    }
                }
            }
            throw new RuntimeException("获取分布式锁超时: " + lockKey);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new RuntimeException("获取分布式锁被中断", e);
        }
    }

    /**
     * 公平锁（多个线程按请求顺序获取锁，适合排队场景）
     */
    public <T> T executeWithFairLock(String lockKey, Supplier<T> supplier) {
        RLock fairLock = redissonClient.getFairLock(lockKey);
        try {
            if (fairLock.tryLock(5, TimeUnit.SECONDS)) {
                try {
                    return supplier.get();
                } finally {
                    if (fairLock.isHeldByCurrentThread()) {
                        fairLock.unlock();
                    }
                }
            }
            throw new RuntimeException("获取公平锁失败: " + lockKey);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new RuntimeException("获取公平锁被中断", e);
        }
    }
}
```

### 4.2 分布式锁应用场景

| 场景 | 锁 Key 格式 | 说明 |
|------|------------|------|
| 库存扣减 | `lock:stock:{skuId}` | 防止并发超卖 |
| 订单创建 | `lock:order:{userId}` | 同一用户防止重复下单 |
| 优惠券领取 | `lock:coupon:{couponId}:{userId}` | 防止重复领取 |
| 余额扣减 | `lock:balance:{userId}` | 防止并发扣款 |
| 号源锁定 | `lock:appointment:{scheduleId}` | 防止挂号超卖 |
| 关注/点赞 | `lock:follow:{userId}:{targetId}` | 防止重复操作 |

---

## 五、RabbitMQ 消息队列方案（通用设计，所有方案共用）

### 5.1 核心使用场景

| 场景 | 交换机类型 | 说明 |
|------|----------|------|
| 异步通知 | Topic | 订单创建后异步发送短信/邮件通知 |
| 延迟任务 | Direct + TTL + 死信队列 | 订单超时自动取消、延迟发货提醒 |
| 流量削峰 | Direct | 秒杀请求入队，后台异步处理 |
| 数据同步 | Fanout | 商品更新后同步到搜索服务/清除缓存 |
| 日志收集 | Topic | 操作日志异步写入数据库 |

### 5.2 消息可靠投递配置

```java
@Configuration
public class RabbitMQConfig {

    // ==================== 延迟队列（订单超时取消）====================

    @Bean
    public Queue orderDelayQueue() {
        // 延迟队列：消息到期后自动转入死信交换机
        return QueueBuilder.durable("order.delay.queue")
                .deadLetterExchange("order.dead.exchange")
                .deadLetterRoutingKey("order.cancel")
                .ttl(1800000) // 30 分钟过期
                .build();
    }

    @Bean
    public Queue orderDeadQueue() {
        // 死信队列：消费者监听此队列处理取消逻辑
        return QueueBuilder.durable("order.dead.queue").build();
    }

    @Bean
    public DirectExchange orderDeadExchange() {
        return new DirectExchange("order.dead.exchange");
    }

    @Bean
    public Binding orderDeadBinding(Queue orderDeadQueue, DirectExchange orderDeadExchange) {
        return BindingBuilder.bind(orderDeadQueue).to(orderDeadExchange).with("order.cancel");
    }

    // ==================== 异步通知队列 ====================

    @Bean
    public TopicExchange notificationExchange() {
        return new TopicExchange("notification.exchange");
    }

    @Bean
    public Queue smsQueue() {
        return QueueBuilder.durable("notification.sms.queue").build();
    }

    @Bean
    public Queue emailQueue() {
        return QueueBuilder.durable("notification.email.queue").build();
    }

    @Bean
    public Binding smsBinding(Queue smsQueue, TopicExchange notificationExchange) {
        return BindingBuilder.bind(smsQueue).to(notificationExchange).with("notification.sms");
    }

    @Bean
    public Binding emailBinding(Queue emailQueue, TopicExchange notificationExchange) {
        return BindingBuilder.bind(emailQueue).to(notificationExchange).with("notification.email");
    }
}
```

### 5.3 消息生产者与消费者示例

```java
// ==================== 生产者 ====================
@Service
public class OrderMessageProducer {

    @Autowired
    private RabbitTemplate rabbitTemplate;

    /** 发送延迟消息（订单超时取消） */
    public void sendOrderDelayMessage(String orderId, long delayMillis) {
        rabbitTemplate.convertAndSend(
            "order.delay.exchange", "order.delay", orderId,
            message -> {
                message.getMessageProperties().setExpiration(String.valueOf(delayMillis));
                return message;
            }
        );
    }

    /** 发送通知消息（短信/邮件） */
    public void sendNotification(String type, NotificationMessage message) {
        rabbitTemplate.convertAndSend(
            "notification.exchange", "notification." + type, message
        );
    }
}

// ==================== 消费者 ====================
@Component
@Slf4j
public class OrderCancelConsumer {

    /** 监听死信队列，处理订单超时取消 */
    @RabbitListener(queues = "order.dead.queue")
    public void handleOrderCancel(String orderId, Message message, Channel channel) {
        try {
            // 1. 检查订单状态（已支付则忽略）
            // 2. 取消订单 + 恢复库存
            // 3. 手动 ACK 确认
            channel.basicAck(message.getMessageProperties().getDeliveryTag(), false);
        } catch (Exception e) {
            try {
                // 拒绝消息，不重新入队（进入人工处理队列）
                channel.basicReject(message.getMessageProperties().getDeliveryTag(), false);
            } catch (IOException ex) {
                log.error("拒绝消息失败", ex);
            }
        }
    }
}
```

---

## 六、产品方案详情

---

### 方案一：🛒 电商商城系统（推荐指数：⭐⭐⭐⭐⭐）

#### 产品概述

B2C 模式综合电商平台，涵盖用户端商城和管理后台，支持商品浏览、购物车、订单、支付、物流等完整购物流程。面试中最常见、最能体现微服务架构能力的项目。

#### 核心功能模块

```
电商商城
├── 用户服务 (user-service)
│   ├── 注册 / 登录 / JWT 鉴权
│   ├── 用户信息管理 / 收货地址管理
│   └── 会员等级体系
├── 商品服务 (product-service)
│   ├── 商品分类（三级分类树）
│   ├── SPU / SKU 管理
│   ├── 商品搜索（关键词 + 筛选）
│   ├── 库存管理
│   └── 商品评价
├── 订单服务 (order-service)
│   ├── 购物车（Redis Hash 实现）
│   ├── 订单创建 / 支付 / 取消
│   ├── 订单状态机流转
│   └── 超时自动取消（RabbitMQ 延迟队列）
├── 营销服务 (promotion-service)
│   ├── 优惠券发放与核销
│   ├── 限时秒杀活动
│   ├── 满减 / 折扣活动
│   └── 积分体系
├── 网关服务 (gateway-service)
│   ├── 统一路由转发
│   ├── JWT 鉴权过滤器
│   ├── 限流（令牌桶）
│   └── 跨域处理
└── 后台管理前端 (admin-web)
    ├── 商品管理面板 / 订单管理面板
    ├── 用户管理面板 / 营销管理
    └── 数据统计大屏
```

#### 技术亮点

| 亮点 | 技术方案 |
|------|---------|
| 秒杀防超卖 | Redisson 分布式锁 + Redis 预扣减 + RabbitMQ 异步下单 |
| 订单超时取消 | RabbitMQ 死信队列，30 分钟未支付自动取消 + 恢复库存 |
| 商品缓存 | 多级缓存（Caffeine + Redis）+ 防穿透/击穿/雪崩完整方案 |
| 异步解耦 | 下单后 MQ 异步：发短信、扣积分、更新销量统计 |
| 接口幂等 | 订单创建 Token 机制，防止重复提交 |
| Docker 部署 | docker-compose 一键编排所有中间件和微服务 |

---

### 方案二：📚 在线教育平台（推荐指数：⭐⭐⭐⭐⭐）

#### 产品概述

面向 K12 或职业技能的在线教育平台，支持课程发布、视频点播、学习追踪、考试练习等功能。业务复杂度适中，能体现分布式系统设计能力。

#### 核心功能模块

```
在线教育平台
├── 用户服务 (user-service)
│   ├── 注册 / 登录 / JWT 鉴权
│   ├── 角色管理（学员 / 讲师 / 管理员）
│   └── 讲师认证与个人主页
├── 课程服务 (course-service)
│   ├── 课程分类（多级分类树）
│   ├── 课程发布 / 编辑 / 上下架
│   ├── 章节 & 课时管理
│   └── 课程搜索与筛选
├── 资源服务 (resource-service)
│   ├── 视频上传 / 存储（对接 OSS）
│   ├── 视频点播（VOD）
│   ├── 课件上传下载
│   └── 资源访问鉴权
├── 学习服务 (study-service)
│   ├── 课程购买 / 收藏
│   ├── 学习进度追踪（Redis 记录）
│   ├── 课时播放记录
│   └── 学习笔记
├── 考试服务 (exam-service)
│   ├── 题库管理（单选 / 多选 / 判断）
│   ├── 在线考试 / 自动判卷
│   ├── 错题本 / 模拟考试
├── 网关服务 (gateway-service)
│   └── 路由 / 鉴权 / 限流
└── 前端：学员端 (student-web) + 管理后台 (admin-web)
```

#### 技术亮点

| 亮点 | 技术方案 |
|------|---------|
| 视频播放 | 对接阿里云 VOD，加密播放 + 防盗链 |
| 学习进度 | Redis 记录播放进度 + RabbitMQ 异步批量同步 MySQL（削峰） |
| 课程缓存 | 多级缓存 + 防穿透/击穿/雪崩完整方案 |
| 购买事务 | 可靠消息最终一致性（MQ 保证订单 + 权益数据一致） |
| 异步转码 | 视频上传后 MQ 异步触发转码任务 |
| Docker 部署 | docker-compose 一键编排所有中间件和微服务 |

---

### 方案三：🏥 互联网医院 / 在线问诊系统（推荐指数：⭐⭐⭐⭐）

#### 产品概述

在线医疗问诊平台，患者可以浏览医生信息、在线挂号、图文问诊、查看检查报告。业务场景独特，面试中辨识度高，不容易与其他候选人撞车。

#### 核心功能模块

```
互联网医院
├── 用户服务 (user-service)
│   ├── 患者注册 / 登录 / 实名认证
│   ├── 就诊人管理（支持多个家庭成员）
│   └── 健康档案管理
├── 医生服务 (doctor-service)
│   ├── 医生注册 / 资质认证
│   ├── 科室 / 医院管理
│   ├── 医生排班管理 / 号源管理
├── 挂号服务 (appointment-service)
│   ├── 在线预约挂号
│   ├── 号源锁定与释放（分布式锁）
│   ├── 挂号状态流转 / 记录查询
├── 问诊服务 (consultation-service)
│   ├── 图文问诊（消息 + 图片）
│   ├── 问诊状态管理 / 处方开具
│   └── 问诊评价
├── 通知服务 (notification-service)
│   ├── 短信通知（RabbitMQ 异步发送）
│   ├── 站内信 / WebSocket 实时推送
├── 网关服务 (gateway-service)
│   └── 路由 / 鉴权 / 限流
└── 前端：患者端 (patient-web) + 医生工作台 (doctor-web)
```

#### 技术亮点

| 亮点 | 技术方案 |
|------|---------|
| 号源防抢 | Redisson 分布式锁 + Lua 脚本原子扣减 |
| WebSocket | 医患实时图文问诊聊天 |
| 消息通知 | 挂号成功 MQ 异步发短信、更新排班统计 |
| 缓存设计 | 医生/排班缓存 + 防穿透/击穿/雪崩完整方案 |
| 数据安全 | 患者敏感信息（手机号、身份证）AES 加密存储 |
| Docker 部署 | docker-compose 一键编排所有中间件和微服务 |

---

### 方案四：🏢 企业级 OA 协同办公系统（推荐指数：⭐⭐⭐⭐）

#### 产品概述

面向中小企业的协同办公平台，包含审批流程、考勤管理、项目管理、文档协作等模块。贴近企业真实需求，能展示复杂业务建模能力。

#### 核心功能模块

```
OA 协同办公
├── 用户权限服务 (auth-service)
│   ├── 员工账号管理
│   ├── RBAC 权限模型（角色 - 权限 - 菜单）
│   ├── 部门组织架构（树形结构）
│   └── 数据权限控制（只看本部门数据）
├── 审批服务 (approval-service)
│   ├── 审批模板配置（请假 / 报销 / 出差）
│   ├── 审批流程引擎（多级审批、会签、或签）
│   ├── 审批记录与追踪
│   └── 审批消息通知（RabbitMQ 异步）
├── 考勤服务 (attendance-service)
│   ├── 上下班打卡（GPS 定位）
│   ├── 考勤规则配置 / 排班管理
│   └── 考勤统计报表
├── 项目服务 (project-service)
│   ├── 项目创建与成员管理
│   ├── 任务看板（待办 / 进行中 / 已完成）
│   ├── 任务分配与截止日期 / 工时统计
├── 文档服务 (document-service)
│   ├── 文件上传 / 下载 / 在线预览
│   └── 目录与权限管理
├── 网关服务 (gateway-service)
│   └── 路由 / 鉴权 / 限流
└── 前端 (oa-web) — 工作台 / 审批 / 考勤 / 项目 / 文档
```

#### 技术亮点

| 亮点 | 技术方案 |
|------|---------|
| 流程引擎 | 自研简易工作流，支持多级审批、会签、或签、条件分支 |
| 数据权限 | 注解 + AOP 实现行级数据权限隔离 |
| 消息通知 | 审批流转 RabbitMQ 异步通知相关人员 |
| 异步导出 | 考勤报表异步生成 + Redis 通知下载（线程池 + MQ） |
| 缓存设计 | 组织架构/权限数据缓存 + 防穿透/击穿/雪崩方案 |
| Docker 部署 | docker-compose 一键编排所有中间件和微服务 |

---

### 方案五：🎵 内容社区 / 短内容平台（推荐指数：⭐⭐⭐⭐）

#### 产品概述

类小红书/微博的短内容社区平台，用户可以发布图文动态、关注其他用户、点赞评论、浏览推荐 Feed 流。高并发场景丰富，能充分展示技术深度。

#### 核心功能模块

```
内容社区
├── 用户服务 (user-service)
│   ├── 注册 / 登录 / JWT 鉴权
│   ├── 个人资料管理
│   ├── 关注 / 粉丝（双向关系）
│   └── 用户认证（普通 / 创作者 / V 认证）
├── 内容服务 (content-service)
│   ├── 图文动态发布 / 编辑 / 删除
│   ├── 图片上传与裁剪（OSS 存储）
│   ├── 话题 / 标签管理
│   └── 内容审核（敏感词 DFA 过滤）
├── 互动服务 (interaction-service)
│   ├── 点赞 / 取消点赞（分布式锁防重复）
│   ├── 评论 / 回复（多级评论树）
│   ├── 收藏 / 收藏夹管理
│   └── @ 提醒
├── Feed 流服务 (feed-service)
│   ├── 关注 Feed（只看关注的人）
│   ├── 推荐 Feed（热门 / 算法推荐）
│   └── Feed 流分页（游标分页）
├── 通知服务 (notification-service)
│   ├── 点赞/评论/关注通知（RabbitMQ 异步）
│   ├── 系统公告 / WebSocket 实时推送
├── 网关服务 (gateway-service)
│   └── 路由 / 鉴权 / 限流
└── 前端 (community-web) — Feed / 发布 / 主页 / 消息
```

#### 技术亮点

| 亮点 | 技术方案 |
|------|---------|
| Feed 流 | 推拉结合 — 大 V 拉模式，普通用户推模式（写扩散 + 读扩散） |
| 计数缓存 | 点赞/评论数 Redis 缓存 + RabbitMQ 异步批量同步 MySQL |
| 分布式锁 | 点赞/关注操作 Redisson 锁防重复 |
| 多级评论 | 评论树结构设计（路径枚举），支持无限层级 |
| 缓存设计 | Feed 数据缓存 + 防穿透/击穿/雪崩完整方案 |
| Docker 部署 | docker-compose 一键编排所有中间件和微服务 |

---

## 七、方案对比总结

| 维度 | 方案一：电商商城 | 方案二：在线教育 | 方案三：互联网医院 | 方案四：OA 办公 | 方案五：内容社区 |
|------|:-:|:-:|:-:|:-:|:-:|
| **面试辨识度** | 中（最常见） | 高 | 非常高 | 高 | 高 |
| **业务复杂度** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **技术深度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **高并发场景** | 秒杀、库存 | 视频播放 | 挂号抢号 | 较少 | Feed 流、点赞 |
| **开发周期** | 4-6 周 | 3-5 周 | 4-5 周 | 4-6 周 | 3-5 周 |
| **适合岗位** | 后端/全栈 | 后端/全栈 | 后端 | 后端/全栈 | 后端 |

---

## 八、推荐选择建议

| 你的情况 | 推荐方案 | 理由 |
|---------|---------|------|
| 想找后端岗位 | **方案一（电商）** 或 **方案五（内容社区）** | 高并发场景多，面试有话聊 |
| 想有差异化 | **方案三（互联网医院）** | 不容易撞车，面试官印象深刻 |
| 想展示全栈能力 | **方案二（在线教育）** 或 **方案四（OA 办公）** | 前端页面丰富，展示面广 |
| 时间紧张 | **方案二（在线教育）** | 核心功能精简后开发周期最短 |
